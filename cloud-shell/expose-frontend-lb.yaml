apiVersion: v1
kind: Service
metadata:
  name: teamflow-frontend-lb
  namespace: teamflow
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: teamflow-frontend

  