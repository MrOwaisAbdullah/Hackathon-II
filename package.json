{
  "name": "teamflow-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "teamflow-web/frontend"
  ],
  "scripts": {
    "build": "cd teamflow-web/frontend && npm run build",
    "vercel-build": "cd teamflow-web/frontend && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
